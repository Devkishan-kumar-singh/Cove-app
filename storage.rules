rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Uploads are authorized by the Express server after checking room membership.
    match /{allPaths=**} { allow read, write: if false; }
  }
}
