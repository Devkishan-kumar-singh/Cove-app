rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Cove accesses Firestore only through the trusted Express server.
    match /{document=**} { allow read, write: if false; }
  }
}
